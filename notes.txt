  Задачей называется директория, в которой содержится хотя бы что-то одно из
следующего:
  — файл .datalog (в нём t.sh-лог изменений задачи)
  — файл problem.properties (в нём, в упрощённом ini-формате всякие параметры
задачи)
  — файл problem.xml (пока что не поддерживается)
  — директория tests (в ней содержатся тесты либо генераторы для тестов)
  — директория source или src (в ней содержатся генераторы для тестов и
прочее)
Приоритеты:
  — Если существует problem.xml, используется он, .datalog синхронизируется по
нему
  …

Пример задачи с tests.7z (и больше ничем): ru.lksh.2015.03-winter.AS.collection
Пример задачи с problem.xml из полигона: ru.lksh.2015.02-august.A.exam.balls

  Мысли про задачи в целом.
Если добавлять к задаче глобальный идентификатор, то это может быть случайный
набор байт (неинтересно), или хеш какого-то параметра задачи. В качестве этого
параметра интересно может быть:
  — первая версия условия
  — stdesc.txt

Можно ещё посмотреть https://acm.math.spbu.ru/svn/specifications (искать в
~/contests/work/archive)
