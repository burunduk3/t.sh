== t.sh todo ==
  1) better finding of validator (in ../src/)
  2) t.sh under cygwin
  3) refactor
  4) rename function tsh_information into something more laconical
  5) ???
  6) profit!
== critical features (implemented in some versions of t.sh, but not in general) ==
  1) [done] -D __T_SH__
  2) [done] .exe suffix
  3) [done] stack for windows in gcc
== misc ==
  add asserts like
  if [ "$UNICODE" == "" ]; then
    echo "No unicode support!"
    exit 1
  fi
